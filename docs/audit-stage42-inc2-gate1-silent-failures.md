# Stage 42 Inc 2 Gate-1 — Silent-Failure Audit
Date: 2026-05-17
Scope: git diff 6f818e4..HEAD (Stage 42 Inc 0/1 ship + Inc 1 hotfix + Stage 42 closure; Stage 43 Inc 0/1 also present in HEAD but OUT OF SCOPE per gate target — only assessed for non-interference with Stage 42 closure invariants)
HEAD: e474c171e4ba4a695ac45f4f3e025935f4614fb2

## Verdict
GATE CLEAN

## Findings (HIGH / MEDIUM / LOW, with confidence 0-100)

No HIGH or MEDIUM findings at confidence >= 80.

### L1 [LOW conf 82] Dogfood header claims "all 5 semantic-type families" but exercises only 4 (memory tier deliberately omitted)

**Citation**: `helixc/examples/dogfood_15_agi_planning_loop.hx:3-5`. The header reads "AGI planning-loop scenario exercising all 5 semantic-type families (memory + spatial + temporal + modal + causal) in a single coherent program."

**Reasoning**: The dogfood body (lines 38-72) builds and unwraps a 4-deep wrapper stack — `Known` (modal) + `Present`/`Future` (temporal) + `WorldFrame` (spatial) + `Cause`/`Effect` (causal). The memory-tier family (`WorkingMem` / `EpisodicMem` / `SemanticMem` from Stage 37) is NOT exercised in any wrapper position. The progress doc `docs/stage42-progress-2026-05-17.md:36-38` explicitly justifies the omission: "5-stack composition probe, omitting the memory wrapper which would make it 6 levels deep but adds no demonstration value beyond the 5 already exercised." The header comment in the .hx file does not mention this omission and asserts the opposite. The `expects` string in `run.py:112` correctly says "4 wrapper layers" — the documentation discrepancy is localized to the .hx header.

**Why LOW not MEDIUM**: this is a documentation-level inaccuracy, not a behavior bug. The witness still validates what it actually tests; the type system still rejects category mistakes for the 4 families it does exercise. The progress doc is internally consistent with the code; only the dogfood's self-description disagrees.

**Why retained as LOW (not OBS)**: a future reader treating this dogfood as the canonical "quintet cohesion" demonstration would incorrectly conclude the memory tier composes orthogonally with the other 4 — when in fact Stage 42 ships NO end-to-end demonstration of that composition. If a Stage 43+ refactor were to break memory-tier composition with another family (e.g., `WorkingMem<Cause<i32>>` rejection), this dogfood would NOT catch it despite the header claim.

**Remediation (~6 LOC)**: amend lines 3-5 to "exercising 4 of the 5 semantic-type families (spatial + temporal + modal + causal); the memory tier is omitted to keep the wrapper stack at 4 deep — see docs/stage42-progress-2026-05-17.md for the justification." OR ship a follow-up dogfood that adds a memory-tier wrapper at the bottom of the stack (genuinely 5-deep) and close the gap.

## OUT OF SCOPE — observations (no severity)

- **O1** (test-suite gap, family-symmetric carry-over from Stage 38 O1 / Stage 39 O1 / Stage 40 O1 / Stage 41 O1). `helixc/tests/test_reflection.py` has NO entry for `test_dogfood_15_agi_planning_loop`. Verified via grep: zero matches for `dogfood_(13|14|15)` in that file. Stage 40 O1 + Stage 41 O1 already documented this gap for dogfood_13 and dogfood_14; Stage 42 inherits it doubled to triple. A regression that broke dogfood_15 end-to-end (e.g., a `propagate` lowering bug, a self-host parser regression on the 4-deep wrapper-stack type literal, a `run.py` DEMOS-dict load-failure, a witness-arithmetic regression in the 3-observation product gate) would not be caught by any Stage 42 test — the dogfood breaks unnoticed because nothing references it from CI. Fix: ~7 LOC in test_reflection.py per dogfood; consolidate the 3-stage backlog (dogfood_13 + dogfood_14 + dogfood_15) into one ~21 LOC sweep.

- **O2** (witness-stride sufficiency, contrasted with the Stage 41 LOW-1 closure decision). dogfood_15 uses inputs 10/14/18 with stride 4. Stage 41 closure gate-1 LOW-1 upgraded its probe from input `1` to input `7` because `1` was a fixpoint of many identity-laundering bugs (e.g., `x*x` at x=1 is 1, `x+x-x` at x=1 is 1, etc.). Stage 42's choice of 10/14/18 is stronger than 1 — none of 10/14/18 are common arithmetic fixpoints, all three are pairwise differentiable by ≥4, and the per-observation `==` check rules out the swap-permutation degenerate case (a bug that returned `[14, 18, 10]` would fail all three binary witnesses). The witness is collapse-resistant to: identity-lowering bugs at any of 21 wrapper operations (any one returning a constant or non-identity transform fails ≥1 binary witness, multiplicative product collapses to 0); a `propagate(c)` bug returning `Effect<0>` (confirmed: from_effect would return 0, fails `==10` check, all_ok=0, exit=0); a `from_effect(eff)` bug returning a different inner field (no other fields exist in Phase-0 Effect<i32> beyond the identity-lowered inner — but even hypothetically, the wrong value fails `==`). The one residual class NOT caught by 10/14/18 stride-4: a paired bug where `into_X` and `from_X` for the SAME kind have coordinated inverse offsets (e.g., `into_world` adds 1, `from_world` subtracts 1, cancelling within the wrap-unwrap pair). This bug-class is rare and would more likely surface at the typecheck/codegen contract layer than at runtime, but the witness alone wouldn't detect it. Borderline OBS — not retained as a finding because the bug class is extremely narrow and not actually plausible given Phase-0's identity-lowering convention (all 21 wrapper builtins lower to `return self._lower_expr(expr.args[0])`, a single shared IR arm at `lower_ast.py:2020-2033` — there is no per-kind code path that could acquire a coordinated paired offset).

- **O3** (run.py DEMOS dict — no key collision, no harness ambiguity). Verified all 15 DEMOS keys are unique (no duplicates). The `expects` string accurately describes the dogfood behavior (4 wrapper layers; matches code). No harness exit-code-mismatch failure mode — `run.py` prints the actual exit code unconditionally at line 190 (`print(f"  -> exit code {code}")`) regardless of whether it matches the expected value. This is by design — `run.py` is a demonstration runner, not a verifier; verification belongs in the test suite (see O1).

- **O4** (typecheck.py restoration — verified intact). The user's verification expectation ("`git show HEAD:helixc/frontend/typecheck.py | sha256sum` should match the blob hash from `6f818e4`'s typecheck.py") is no longer literally satisfiable because Stage 43 Inc 1 (`e474c17`, OUT OF SCOPE but in HEAD) added arity-diagnostic arms that intentionally diverge the blob. The equivalent verification — that the Stage 42 closure state (`8c30b76`) matches `6f818e4`'s typecheck.py — IS satisfied: both blobs hash to `cc800c2653da8aed5ebab88697af524d24080023b1a0cf9243c4972927d791ae`. The Stage 41 closure F1 fix (`inner_is_shadowed` predicate at typecheck.py:3828-3841 and the parallel `_causal_intro` site at 3938-3941) is present at HEAD. The 6 safety-anchored reverse-direction hints in `_causal_upgrade_hint` (`effect→cause`, `joint→cause`, `independent→cause`, `independent→joint`, `independent→effect`, plus the 3 forward audited transitions) are present at `typecheck.py:3852-3899`. Stage 43 Inc 1's edits are purely additive arity diagnostics and double-wrap rejection — they do not touch the gate-1 F1 fix lines.

## Verification steps performed

- Diffed full Stage 42 surface via `git diff --stat 6f818e4..HEAD`: 8 files / 1587 insertions / 2 deletions. Of these, 3 files are in scope for Stage 42 Inc 2 (dogfood_15, run.py, stage42-progress); 3 are retrospective Stage 41 audit docs (informational only); 2 are Stage 43 ledger + cleanup (out of scope, but verified non-interfering).
- Read dogfood_15 end-to-end (all 85 lines). Traced raw input value through 21 wrapper operations (3 into_* + 2 from_* + 1 from_* + 4 into_* + 3 from_* + 1 propagate + 3 into_* + 3 from_* + 1 from_*) to confirm identity preservation requires all 21 ops to be identity-lowered correctly.
- Executed `python -m helixc.examples.run planning` from project root — exit code 42 confirmed live.
- Verified DEMOS dict key uniqueness via Python regex extraction of `^    "(\w+)":` patterns — 15 unique keys, zero duplicates, no `"planning"` collision with any existing key.
- Diffed `run.py` change (+5 lines) — purely additive DEMOS entry; no harness-logic change; no exit-code-comparison change.
- Hashed typecheck.py blob at three commits: `6f818e4` (Stage 41 CLOSED), `8c30b76` (Stage 42 CLOSED), `HEAD` (Stage 43 Inc 1). First two are identical (`cc800c26...`); HEAD differs (`bd830183...`) solely due to Stage 43 Inc 1 additions verified via line-diff to be additive-only.
- Re-read typecheck.py:3815-3855 at HEAD to verify the F1 `inner_is_shadowed` predicate is present at the causal-launder site. Confirmed: predicate at lines 3828-3834, guard application at 3835-3841 includes `and not inner_is_shadowed`.
- Re-read typecheck.py:3852-3899 to verify the 6 safety-anchored reverse-direction hints. Confirmed: `effect→cause`, `joint→cause`, `independent→cause`, `independent→joint`, `independent→effect` all present with safety-anchored prose; plus the 3 forward audited transitions (`cause→effect` via propagate, `effect→joint` via aggregate, `joint→independent` via isolate).
- Considered the Stage 41 LOW-1 "degenerate witness" parallel: Stage 42 uses 10/14/18 instead of 1, stride 4. Enumerated bug-classes the witness must detect (identity-lowering per layer, value-drift via propagate, swap-permutation, constant-return, paired offset) and confirmed 4 of 5 are caught with high probability; the 5th (paired offset on the SAME kind's intro/elim) is architecturally precluded by the shared single-arm IR lowering at `lower_ast.py:2020-2033`.
- Cross-checked the .hx header claim of "all 5 semantic-type families" against the actual wrapper inventory in the function body — memory tier is missing from the 4-wrapper stack despite the header listing it (L1 finding).
- Confirmed `expects` string in `run.py:112` accurately says "4 wrapper layers" — no harness-level misclaim.
- Grepped `helixc/tests/test_reflection.py` for `dogfood_(13|14|15)` — zero matches, confirming O1 family-symmetric carry-over.
- Confirmed Stage 41 audit `audit-stage41-inc4-gate1-silent-failures.md` HIGH F2 (cross-family modal/causal launder) is NOT addressed by Stage 42 — but it is OUT OF SCOPE for a Stage 42 audit; Stage 42 shipped no typecheck changes (the Inc 1 hotfix only restored Stage 41's already-merged fixes). The cross-family F2 remains a Stage 41 inheritance and is not a Stage 42 silent-failure regression.
- Confirmed Stage 42 closure progress doc (`stage42-progress-2026-05-17.md:49-55`) accurately describes Stage 42 as "SLIM stage: no new type primitives or compiler-side changes" — consistent with the actual diff (only dogfood + ledger + run.py registration + the Inc 1 hotfix that restored a pre-existing Stage 41 fix).
